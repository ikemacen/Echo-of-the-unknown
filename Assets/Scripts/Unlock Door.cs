using UnityEngine;

public class UnlockDoor : MonoBehaviour
{
    /*public InventorySystem inventoryCheck;
    public bool hasKey;
    public GameObject key;

    void Start()
    {
        inventoryCheck = gameObject.GetComponent<InventorySystem>();
    }

    void OnTriggerEnter(Collider other)
    {
        if(other.gameObject.CompareTag("Player"))
        {
            for(int i = 0; i < inventoryCheck.inventory.Count; i++)
            {
                if (inventoryCheck.inventory.Contains(key))
                {
                    //text mesh "Interact to Unlock Door"
                }
                else
                {
                    //text mesh blank
                }
            }
            
        }
    }

    void OnTriggerExit(Collider other)
    {
        //text mesh disabled
    }

    void Update()
    {
        //interact is clicked, text changes to "Door is now unlocked"
    }*/
}
